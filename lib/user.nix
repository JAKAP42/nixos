# Central place for the few facts that identify this user/host. Threaded into
# every module as `userconf` (via specialArgs / extraSpecialArgs), so values
# live in exactly one spot.
{
  username = "jakap42";
  displayname = "Jacob Sjølie";
  host = "nixos";
  # Keep system + home stateVersion in sync; do NOT bump casually.
  stateVersion = "26.05";
}
